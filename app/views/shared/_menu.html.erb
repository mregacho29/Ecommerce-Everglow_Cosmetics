<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <!-- Brand Logo and Name -->
    <%= link_to root_path, class: 'navbar-brand d-flex align-items-center' do %>
      <img src="/path/to/logo.png" alt="Everglow Cosmetics" class="me-2" style="height: 40px;">
      <span>Everglow Cosmetics</span>
    <% end %>

    <!-- Navbar Toggler for Mobile -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar Content -->
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <!-- Left Side: Search Bar -->
      <form class="d-flex me-auto" action="<%= search_path %>" method="get">
        <input class="form-control me-2" type="search" name="query" placeholder="Search products..." aria-label="Search">
        <button class="btn btn-outline-light" type="submit">Search</button>
      </form>

      <!-- Right Side: Links and Icons -->
      <ul class="navbar-nav mb-2 mb-lg-0">
        <!-- Dropdown for Stores and Services -->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="storesDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="bi bi-shop"></i> Stores & Services
          </a>
          <ul class="dropdown-menu" aria-labelledby="storesDropdown">
            <li><%= link_to 'Store Locator', stores_path, class: 'dropdown-item' %></li>
            <li><%= link_to 'Services', services_path, class: 'dropdown-item' %></li>
          </ul>
        </li>

        <!-- Sign In Link -->
        <li class="nav-item">
          <%= link_to 'Sign In', new_user_session_path, class: 'nav-link' %>
        </li>

        <!-- Cart Icon -->
        <li class="nav-item">
          <%= link_to cart_path, class: 'nav-link' do %>
            <i class="bi bi-cart"></i> Cart
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>